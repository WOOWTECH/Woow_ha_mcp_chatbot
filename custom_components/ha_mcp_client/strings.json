{
  "config": {
    "step": {
      "user": {
        "title": "n8n Connection",
        "description": "Enter n8n base URL and optional API key for auto-discovery of Chat Trigger workflows",
        "data": {
          "n8n_base_url": "n8n Base URL",
          "n8n_api_key": "n8n API Key (read-only, optional)"
        }
      },
      "select_workflow": {
        "title": "Select Chat Workflow",
        "description": "Detected workflows with Chat Trigger nodes:",
        "data": {
          "workflow": "Workflow"
        }
      },
      "webhook": {
        "title": "Webhook Path",
        "description": "Enter the production webhook path from the Chat Trigger node (e.g. webhook/xxxx/chat)",
        "data": {
          "n8n_webhook_path": "Webhook Path"
        }
      },
      "auth": {
        "title": "Webhook Authentication",
        "description": "Enter Basic Auth credentials configured on the Chat Trigger node",
        "data": {
          "n8n_basic_user": "Username",
          "n8n_basic_pass": "Password"
        }
      }
    },
    "error": {
      "no_chat_workflows": "No active workflows with Chat Trigger found",
      "auth_required": "Username and password are both required",
      "webhook_unreachable": "Cannot reach webhook endpoint, check URL and credentials"
    },
    "abort": {
      "already_configured": "HA MCP Client is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HA MCP Client Settings",
        "description": "Update n8n connection and conversation history options",
        "data": {
          "n8n_base_url": "n8n Base URL",
          "n8n_webhook_path": "Webhook Path",
          "n8n_basic_user": "Username",
          "n8n_basic_pass": "Password (leave empty to keep current)",
          "enable_conversation_history": "Enable conversation history",
          "history_retention_days": "History retention (days)"
        }
      }
    }
  },
  "services": {
    "clear_conversation_history": {
      "name": "Clear Conversation History",
      "description": "Clear conversation history for a user",
      "fields": {
        "user_id": {
          "name": "User ID",
          "description": "The Home Assistant user ID (leave empty for current user)"
        }
      }
    },
    "export_conversation_history": {
      "name": "Export Conversation History",
      "description": "Export conversation history",
      "fields": {
        "user_id": {
          "name": "User ID",
          "description": "The Home Assistant user ID (leave empty for current user)"
        },
        "format": {
          "name": "Format",
          "description": "Export format (json or markdown)"
        }
      }
    }
  }
}
